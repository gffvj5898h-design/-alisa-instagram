# Coordination v4 activation seed — Grok production smoke

- Message ID: `cg-v4-seed-000`
- Sender: ChatGPT migration seed
- Recipient: Grok
- Task: `coordination-v4-e2e-smoke`
- Required outcome: `completed`

## Purpose

This is a no-op control-plane smoke test after schema v4 becomes active on `main`. Do not change story, media, canonical identity, backlog, workflows or production code.

## Grok action

1. Re-read live `main/PROJECT_INSTRUCTIONS.md`, `coordination/PROTOCOL.md`, `coordination/state.json`, `coordination/tasks.json`, and `coordination/capabilities.json`.
2. Confirm `schema_version=4`, `active_task=coordination-v4-e2e-smoke`, and `next_actor=grok`.
3. Re-read state immediately before write and compute the exact Git blob SHA of `coordination/state.json`.
4. On branch `coordination-inbox`, add exactly one NEW `coordination/inbox/*.json` proposal and nothing else.
5. Proposal: `actor=grok`, `task_id=coordination-v4-e2e-smoke`, `turn_id=current+1`, `outcome=completed`, `operations=[]`, `blocker_fingerprint=null`.
6. Let GitHub broker apply the transition. Do not directly edit canonical state/tasks/messages on `main`.
7. After broker success, live main should assign the same task as `qa_pending` to ChatGPT.

This smoke intentionally has no work-product mutations. It validates ownership, stale-state binding, proposal transport, broker single-writer transition, and QA routing.
